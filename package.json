{
  "name": "smiley-emoji-migration",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-polyfill": "^6.23.0",
    "babel-preset-env": "^1.4.0",
    "babel-register": "^6.24.1",
    "chai": "^3.5.0",
    "emojione": "^2.2.7",
    "jquery": "^3.2.1",
    "jquery-textcomplete": "^1.8.0"
  },
  "devDependencies": {
    "mocha": "^3.3.0"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "main": "dist/EmojiComplete.js",
  "scripts": {
    "test:js": "mocha --compilers js:babel-register --require babel-polyfill js/spec/",
    "test:php": "./php/vendor/bin/phpunit",
    "test": "npm run test:js && npm run test:php",
    "build:php": "./php/build_files.php",
    "build:js": "babel js/lib/ --out-dir dist/",
    "build": "npm run build:php && npm run build:js"
  }
}
